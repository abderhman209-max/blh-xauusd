{
  "name": "blh-xauusd",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "check": "node --check worker/index.js && node --check api/index.js",
    "build": "npm run check && mkdir -p public"
  }
}
